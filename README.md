continues integration using docker

### Some usefull packages i've used :

- `express-ejs-layouts`
- `method-override`: to use PUT and DELETE methods in forms, it rewrite the method to the one specified in the query string
